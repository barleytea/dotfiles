name: NixOS CI
on:
  push:
    branches: [main]
    paths:
      - 'nixos/**'
      - 'modules/**'
      - 'nixvim/**'
      - '.github/workflows/nixos.yml'
      - '.github/actions/setup-nix/**'
  pull_request:
    branches: [main]
    paths:
      - 'nixos/**'
      - 'modules/**'
      - 'nixvim/**'
      - '.github/workflows/nixos.yml'
      - '.github/actions/setup-nix/**'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      id-token: "write"
      contents: "read"

    steps:
      - uses: actions/checkout@v6

      - name: Free disk space
        run: |
          echo "Disk space before cleanup:"
          df -h
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          echo "Disk space after cleanup:"
          df -h

      - name: Setup Nix
        uses: ./.github/actions/setup-nix
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Check NixOS configuration
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NIXVIM_CONFIG_INPUT: path:${{ github.workspace }}/nixvim
        run: |
          export NIX_CONFIG="access-tokens = github.com=$GITHUB_TOKEN"
          cd nixos

          echo "Disk space before NixOS build:"
          df -h

          nix build path:.#nixosConfigurations.desktop.config.system.build.toplevel --impure --no-write-lock-file --override-input nixvim-config "$NIXVIM_CONFIG_INPUT"

          echo "Disk space after NixOS build:"
          df -h

          nix-collect-garbage -d
          nix store optimise

          echo "Disk space after NixOS cleanup:"
          df -h

      - name: Check nixvim configuration
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          export NIX_CONFIG="access-tokens = github.com=$GITHUB_TOKEN"
          cd nixvim

          echo "Disk space before nixvim build:"
          df -h

          nix build .#packages.x86_64-linux.default --impure --no-write-lock-file

          echo "Disk space after nixvim build:"
          df -h

          nix-collect-garbage -d
          nix store optimise

          echo "Disk space after nixvim cleanup:"
          df -h
